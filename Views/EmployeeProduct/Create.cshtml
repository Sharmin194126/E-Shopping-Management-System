@model E_ShoppingManagement.ViewModels.ProductViewModel

@{
    ViewData["Title"] = "Create Product";
    Layout = "~/Views/Shared/_EmployeeLayout.cshtml"; // Employee layout
}

<h2 class="mb-3">Create Product</h2>

<style>
    .page-card {
        background: #f8f9fa;
        padding: 30px;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        color: #333;
    }
    .form-control, .form-select {
        background-color: #fff !important;
        color: #333 !important;
        border: 1px solid #ced4da !important;
    }
    .form-control:focus, .form-select:focus {
        border-color: #80bdff !important;
        outline: 0;
        box-shadow: 0 0 0 0.2rem rgba(0,123,255,.25) !important;
    }
</style>

<div class="page-card">
    <form asp-action="Create" method="post" enctype="multipart/form-data">

        <div class="row">
            <div class="col-md-6">
                <div class="form-group mb-3">
                    <label asp-for="Name" class="form-label">Product Name</label>
                    <input asp-for="Name" class="form-control" placeholder="Enter product name" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
            </div>
            <div class="col-md-3">
                <div class="form-group mb-3">
                    <label asp-for="CategoryId" class="form-label">Category</label>
                    <select asp-for="CategoryId" class="form-select" asp-items="Model.CategoryList">
                        <option value="">Category</option>
                    </select>
                    <span asp-validation-for="CategoryId" class="text-danger"></span>
                </div>
            </div>
            <div class="col-md-3">
                <div class="form-group mb-3">
                    <label asp-for="ProductTypeId" class="form-label">Product Type</label>
                    <select asp-for="ProductTypeId" class="form-select" asp-items="Model.ProductTypeList">
                        <option value=""> Type </option>
                    </select>
                    <span asp-validation-for="ProductTypeId" class="text-danger"></span>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-4">
                <div class="form-group mb-3">
                    <label>Regular Price</label>
                    <input asp-for="RegularPrice" id="regPrice" class="form-control" type="number" step="0.01" />
                    <span asp-validation-for="RegularPrice" class="text-danger"></span>
                </div>
            </div>
            <div class="col-md-4">
                <div class="form-group mb-3">
                    <label>Offer %</label>
                    <input asp-for="OfferPercentage" id="offPercent" class="form-control" type="number" step="0.01" />
                    <span asp-validation-for="OfferPercentage" class="text-danger"></span>
                </div>
            </div>
            <div class="col-md-4">
                <div class="form-group mb-3">
                    <label>Offer Price (Auto)</label>
                    <input asp-for="Price" id="offPrice" class="form-control" readonly style="background-color: #e9ecef;" />
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-4">
                <div class="form-group mb-3">
                    <label asp-for="StockQty" class="form-label">Stock Quantity</label>
                    <input asp-for="StockQty" class="form-control" type="number" />
                    <span asp-validation-for="StockQty" class="text-danger"></span>
                </div>
            </div>
            <div class="col-md-4">
                <div class="form-group mb-3">
                    <label asp-for="VatPercentage" class="form-label">VAT %</label>
                    <input asp-for="VatPercentage" class="form-control" type="number" step="0.01" />
                </div>
            </div>

        <div class="row">
            <div class="col-md-4">
                <div class="form-group mb-3">
                    <label asp-for="DisplayCategory" class="form-label">Home Display Section</label>
                    <select asp-for="DisplayCategory" class="form-select" asp-items="Model.DisplayCategoryList">
                        <option value=""> None </option>
                    </select>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-4">
                <div class="form-group mb-3">
                    <label asp-for="AvailableSizes" class="form-label">Available Sizes (Comma separated)</label>
                    <input asp-for="AvailableSizes" id="availableSizes" class="form-control" placeholder="e.g. L, M, XL" />
                </div>
            </div>
            <div class="col-md-4">
                <div class="form-group mb-3">
                    <label asp-for="Status" class="form-label">Product Status</label>
                    <select asp-for="Status" class="form-select" asp-items="Model.StatusList"></select>
                </div>
            </div>
        </div>

        <div id="sizeStockContainer" class="mt-3 mb-3" style="display:none; background: #fff; padding: 15px; border-radius: 8px; border: 1px solid #ced4da;">
            <label class="fw-bold mb-2">Inventory by Size</label>
            <div id="sizeStockInputs"></div>
            <p class="small text-muted mt-2">Note: Total Stock Quantity will be auto-calculated from these values.</p>
        </div>

        <div class="form-group mb-3">
            <label asp-for="Description" class="form-label"></label>
            <textarea asp-for="Description" rows="3" class="form-control"></textarea>
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>

        <div class="form-group mb-3">
            <label asp-for="ImageFile" class="form-label">Product Image</label>
            <input asp-for="ImageFile" type="file" class="form-control" />
        </div>

        <div class="mt-4 d-flex justify-content-between">
            <a asp-action="Index" class="btn btn-secondary">Back to List</a>
            <button type="submit" class="btn btn-primary">Save</button>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        $(document).ready(function() {
            function calculateOffer() {
                var regPrice = parseFloat($('#regPrice').val()) || 0;
                var offPercent = parseFloat($('#offPercent').val()) || 0;
                var offerPrice = regPrice - (regPrice * (offPercent / 100));
                $('#offPrice').val(offerPrice.toFixed(2));
            }

            $('#regPrice, #offPercent').on('input', function() {
                calculateOffer();
            });

            // Size-wise stock logic
            $('#availableSizes').on('input', function() {
                updateSizeFields();
            });

            function updateSizeFields() {
                var sizesStr = $('#availableSizes').val();
                var container = $('#sizeStockContainer');
                var inputsDiv = $('#sizeStockInputs');
                
                if (!sizesStr || sizesStr.trim() === '') {
                    container.hide();
                    inputsDiv.empty();
                    return;
                }

                var sizes = sizesStr.split(',').map(s => s.trim()).filter(s => s !== '');
                if (sizes.length === 0) {
                    container.hide();
                    inputsDiv.empty();
                    return;
                }

                container.show();
                var existingValues = {};
                inputsDiv.find('input.size-stock-input').each(function() {
                    existingValues[$(this).attr('data-size')] = $(this).val();
                });

                inputsDiv.empty();
                sizes.forEach((size, index) => {
                    var val = existingValues[size] || 0;
                    var html = `
                        <div class="row align-items-center mb-2">
                            <div class="col-4"><span class="badge bg-secondary">${size}</span></div>
                            <div class="col-8">
                                <input type="number" name="SizeStocks[${index}].StockQty" 
                                       data-size="${size}" value="${val}" class="form-control form-control-sm size-stock-input" min="0" />
                                <input type="hidden" name="SizeStocks[${index}].Size" value="${size}" />
                            </div>
                        </div>`;
                    inputsDiv.append(html);
                });

                $('.size-stock-input').off('input').on('input', calculateTotalStock);
            }

            function calculateTotalStock() {
                var total = 0;
                $('.size-stock-input').each(function() {
                    total += parseInt($(this).val()) || 0;
                });
                $('input[name="StockQty"]').val(total);
            }

            calculateOffer();
            updateSizeFields();
        });
    </script>
}
