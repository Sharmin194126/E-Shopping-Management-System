@model E_ShoppingManagement.Models.Customer
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "My Profile";
}

<div class="container mt-5">
    

<div class="row">
        <!-- Sidebar / Menu -->
        <div class="col-md-3">
            @{ ViewData["ActivePage"] = "Profile"; }
            @await Html.PartialAsync("_CustomerSidebar")
        </div>

        <div class="col-md-9">
            <h2 class="mb-4 fw-bold" style="color:#532374;"><i class="bi bi-person-circle me-2"></i>My Profile</h2>
            
            <div class="card shadow-sm border-0" style="border-radius:12px;">
                <div class="card-body p-4">
                    <div class="row mb-4 align-items-center">
                        <div class="col-md-3 fw-bold text-muted">Full Name:</div>
                        <div class="col-md-9 fs-5">@Model.Name</div>
                    </div>
                    <div class="row mb-4 align-items-center">
                        <div class="col-md-3 fw-bold text-muted">Email Address:</div>
                        <div class="col-md-9 fs-5">@Model.Email</div>
                    </div>
                    <div class="row mb-4 align-items-center">
                        <div class="col-md-3 fw-bold text-muted">Account Status:</div>
                        <div class="col-md-9"><span class="badge bg-success px-3 py-2">@Model.Status</span></div>
                    </div>
                     <div class="row mb-4 align-items-center">
                        <div class="col-md-3 fw-bold text-muted">Member Since:</div>
                        <div class="col-md-9 fs-5 text-secondary">@Model.CreatedAt.ToString("dd/MM/yyyy hh:mm tt")</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



