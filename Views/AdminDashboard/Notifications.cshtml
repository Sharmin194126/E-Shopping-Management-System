@{
    ViewData["Title"] = "System Notifications";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    var orders = ViewBag.RecentOrders as List<E_ShoppingManagement.Models.Order>;
    var customers = ViewBag.RecentCustomers as List<E_ShoppingManagement.Models.Customer>;
    var employees = ViewBag.RecentEmployees as List<E_ShoppingManagement.Models.Employee>;
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h3 class="fw-bold mb-0" style="color: #532374;"><i class="bi bi-bell-fill me-2"></i> All Notifications & Activities</h3>
    <a href="javascript:history.back()" class="btn btn-outline-secondary btn-sm px-4 rounded-pill">Back</a>
</div>

<div class="row g-4">
    <!-- Orders Activity -->
    <div class="col-md-6">
        <div class="card border-0 shadow-sm" style="border-radius: 15px;">
            <div class="card-header bg-white py-3 border-0">
                <h5 class="mb-0 fw-bold"><i class="bi bi-cart-check text-primary me-2"></i>Recent Orders</h5>
            </div>
            <div class="card-body p-0">
                <div class="list-group list-group-flush">
                    @if (orders != null && orders.Any())
                    {
                        foreach (var order in orders)
                        {
                            <div class="list-group-item py-3">
                                <div class="d-flex justify-content-between align-items-center">
                                    <div>
                                        <span class="fw-bold">New Order #@order.Id</span>
                                        <div class="small text-muted">Customer: @(order.Customer?.Name ?? "Admin")</div>
                                        <div class="small mt-1">
                                            Status: <span class="badge @(order.OrderStatus == "Delivered" ? "bg-success" : (order.OrderStatus == "Pending" ? "bg-warning text-dark" : "bg-info"))">@order.OrderStatus</span>
                                        </div>
                                    </div>
                                    <div class="text-end">
                                        <div class="small text-muted">@order.CreatedAt.ToString("MMM dd, hh:mm tt")</div>
                                        <a asp-action="OrderDetails" asp-route-id="@order.Id" class="btn btn-sm btn-link text-decoration-none p-0">View</a>
                                    </div>
                                </div>
                            </div>
                        }
                    }
                    else
                    {
                        <div class="p-4 text-center text-muted">No recent orders found.</div>
                    }
                </div>
            </div>
        </div>
    </div>

    <!-- Registration Activities -->
    <div class="col-md-6">
        <div class="card border-0 shadow-sm mb-4" style="border-radius: 15px;">
            <div class="card-header bg-white py-3 border-0">
                <h5 class="mb-0 fw-bold"><i class="bi bi-person-plus text-success me-2"></i>New Registrations</h5>
            </div>
            <div class="card-body p-0">
                <div class="list-group list-group-flush">
                    @if (employees != null && employees.Any())
                    {
                        foreach (var emp in employees)
                        {
                            <div class="list-group-item py-3">
                                <div class="d-flex justify-content-between align-items-center">
                                    <div>
                                        <span class="fw-bold">Employee: @emp.Name</span>
                                        <div class="small text-muted">@emp.Email | @emp.Designation</div>
                                        @if(emp.Status != "Active") {
                                            <span class="badge bg-danger">Pending Approval</span>
                                        }
                                    </div>
                                    <div class="text-end">
                                        <div class="small text-muted">@emp.CreatedAt.ToString("MMM dd, yyyy")</div>
                                        <a asp-controller="Employee" asp-action="Details" asp-route-id="@emp.Id" class="btn btn-sm btn-link text-decoration-none p-0 text-success">Approve</a>
                                    </div>
                                </div>
                            </div>
                        }
                    }
                    @if (customers != null && customers.Any())
                    {
                        foreach (var cust in customers)
                        {
                            <div class="list-group-item py-3">
                                <div class="d-flex justify-content-between align-items-center">
                                    <div>
                                        <span class="fw-bold">Customer: @cust.Name</span>
                                        <div class="small text-muted">Joined: @cust.CreatedAt.ToString("MMM dd, yyyy")</div>
                                    </div>
                                    <div class="text-end">
                                        <i class="bi bi-person text-info" style="font-size: 1.2rem;"></i>
                                    </div>
                                </div>
                            </div>
                        }
                    }
                    else if ((employees == null || !employees.Any()) && (customers == null || !customers.Any()))
                    {
                        <div class="p-4 text-center text-muted">No recent registrations found.</div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
